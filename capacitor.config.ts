import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.optobench.app',
  appName: 'optobench-scaffold-tmp',
  webDir: 'dist'
};

export default config;
